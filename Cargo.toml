cargo-features = ["codegen-backend"]
[package]
name = "social_networks"
version = "0.3.3"
edition = "2024"
authors = ["Valeriy Sakharov <valeratrades@gmail.com>"]
description = "my utils for social networks managing/monitoring"
readme = "README.md"
license = "BlueOak-1.0.0"
repository = "https://github.com/valeratrades/social_networks"

[package.metadata.docs.rs]
cargo-args = ["-Zunstable-options", "-Zrustdoc-scrape-examples"]

[package.metadata.binstall]
pkg-url = "{ repo }/releases/download/v{ version }/{ name }-{ target }.tar.gz"
bin-dir = "{ bin }{ binary-ext }"
pkg-fmt = "tgz"

[dependencies]
ask_llm = "^2.2.2"
base64 = "^0.22.1"
clap = { version = "^4.6.1", features = ["derive"] }
color-eyre = "^0.6.5"
colored = "^3"
config = "^0.15.22"
derive-new = "^0.7.0"
futures = "0.3.32"
futures-util = "^0.3.32"
google-gmail1 = "^7"
grammers-client = "^0.9"
grammers-mtsender = "^0.9"
grammers-session = "^0.9"
grammers-tl-types = "^0.9"
hmac = "^0.13"
hyper = "^1.9"
hyper-rustls = "^0.27"
hyper-util = { version = "^0.1.20", features = ["client", "client-legacy", "http1"] }
imap = "3.0.0-alpha.15"
jiff = { version = "0.2.23", features = ["serde"] }
libsql = "^0.9"
miette = { version = "7.6.0", features = ["fancy", "syntect-highlighter", "serde"] }
oauth1-request = "^0.6"
psm = "0.1.30"
quick-xml = "0.39.2"
rand = "^0.10"
regex = "^1.12.3"
reqwest = { version = "^0.13.2", features = ["json", "multipart", "query", "form"] }
rustls = { version = "^0.23.38", features = ["aws-lc-rs"] }
serde = { version = "^1", features = ["derive"] }
serde_json = "^1"
sha1 = "^0.11"
tg = "^0.9"
tokio = { version = "^1", features = ["full"] }
tokio-tungstenite = { version = "^0", features = ["native-tls"] }
tracing = "^0"
tracing-appender = "^0"
tracing-subscriber = "^0"
v_exchanges = { version = "0.17.1", default-features = false, features = ["binance"] }
v_utils = { version = "^2.15.37", features = ["xdg", "cli", "tracing"] } #ga: rm path
xdg = { version = "3.0.0", features = ["serde"] }
yup-oauth2 = "12"
#,}}}

[dev-dependencies]
insta = "1.47.2"

[lints.clippy]
# Stable
float_cmp = "allow" # is bad for `==` direct comparisons, but `<` and `>` should be allowed
len_zero = "allow" # `.empty()` is O(1) but on &str only
undocumented_unsafe_blocks = "warn"
tabs_in_doc_comments = "allow"

# Questionable
# const fns, so actually more performant
get_first = "allow"
get_last_with_len = "allow"
#,}}}
